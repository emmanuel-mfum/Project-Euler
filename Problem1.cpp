#include<iostream>
#include<vector>

using namespace std;

/*
int main() {


	// Problem 1 : Find all the multiples of 3 or 5 below 1000

	vector<int> numbers;

	for (int i = 0; i < 1000; i++) {

		numbers.push_back(i); // add all integer numbers from 0 to 999 in the vector
	}


	vector<int> multiples;

	for (int i = 0; i < 1000; i++) {

		if (numbers.at(i) % 3 == 0 || numbers.at(i) % 5 == 0) {

			multiples.push_back(numbers.at(i)); // add all integer numbers from 0 to 999 that are multiples of 3 or 5
		}
	}

	int sum = 0;

	for (int i = 0; i < multiples.size(); i++) {

		sum += multiples.at(i); // take the sum of all the multiples of 3 or 5 below 1000
	}

	cout << "The sum of the multiples of 3 or 5 below 1000 is : " << sum << endl;







	return 0;
}

*/